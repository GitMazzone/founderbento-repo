import { FunctionComponent, ReactNode } from 'react';

type {{componentName}}Props = {
  children: ReactNode;
};

export const {{componentName}}: FunctionComponent<{{componentName}}Props> = ({ children }) => {
  return (
    <>
      {children}
    </>
  );
};
